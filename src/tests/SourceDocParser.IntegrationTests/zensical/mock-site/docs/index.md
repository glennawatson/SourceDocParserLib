# SourceDocParser Render Smoke

Mock landing page used by the Zensical render-smoke integration test.

!!! info "What this is"
    A minimal Material/Zensical-flavoured page that exercises the same
    Markdown extensions our `ZensicalDocumentationEmitter` produces:
    admonitions, fenced code blocks, attribute lists, autoref-style links.

See [the sample type page](api/SampleType.md) for the page-shape fixture.
